@import 'jeet'

.login-wrapper

.login
  center(800px)

.register-form
  col(1/2)
  form
    stack()

.login-logo
  col(1/2)
  img
    center()
    animation-iteration-count:infinite;
    animation-duration: 1s;
    animation-delay: 5s;
    max-width:300px
